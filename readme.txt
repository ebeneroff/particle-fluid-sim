ebenerof
Semi-fluid like particles

Written using WebGl, all that is required is opening the index.html file in the browser.
Implements simple advection, diffusion, and force manipulation (via the mouse).
Press Space to generate more particles uniformly across the screen, X to remove all particles,
Drag across screen to manipulate particles.